# Baseline model card

The baseline is a Ridge or LightGBM LambdaRank model trained with purged walk-forward splits. Labels are five-day excess-return relevance grades 0–4. Only out-of-sample metrics may be used for promotion.

