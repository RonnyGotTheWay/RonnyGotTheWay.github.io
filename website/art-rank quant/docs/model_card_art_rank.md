# ART-Rank model card

Status: development candidate. HMM/TFT outputs are lagged context features, PatchTST produces temporal embeddings, and LambdaRank optimizes five-day cross-sectional relevance. Dynamic weights use only realized out-of-sample Rank IC prior to the prediction date. SHAP-style values describe model contribution, not causality. No performance or return is promised.

