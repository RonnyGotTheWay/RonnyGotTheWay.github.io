# Rollback

Rollback means repointing the stable read alias to an existing immutable data/model/publication version. Never edit an old snapshot. Record the operator, reason and affected versions in the audit log.

