# Model promotion

A challenger must pass PIT, cross-regime out-of-sample, cost-adjusted and constraint gates. Eligibility never promotes automatically; a human must review the model card, training window, data version, metrics and artifact checksum.

